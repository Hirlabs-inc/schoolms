# npm can't resolve some shadcn/ui peer ranges against React 19
# (e.g. vaul@0.9.9 declares react ^16.8 || ^17 || ^18). Use legacy peer
# resolution so `npm ci` / `npm install` succeed on CI and servers.
legacy-peer-deps=true
